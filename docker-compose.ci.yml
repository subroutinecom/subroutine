x-common-args: &common-args
  CI: true
  BUILD_SHA: ${BUILD_SHA:-}

services:
  sandbox:
    build:
      target: prod
      args:
        <<: *common-args
  api:
    build:
      target: prod
      args:
        <<: *common-args
  admin-panel:
    build:
      target: prod
      args:
        <<: *common-args
  tests:
    build:
      target: ci
